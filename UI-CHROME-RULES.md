# WINE25+ 공통 모바일 크롬 규칙

## 스테이터스바

- 높이: `60px`
- 배경: 흰색
- 좌우 패딩: 왼쪽 `30px`, 오른쪽 `25px`
- 시간, 다이내믹 아일랜드, 통신·배터리 아이콘은 `.status-bar` 한 구성만 사용한다.
- 전체 화면 이미지에 포함된 스테이터스바는 인터랙티브 UI로 사용하지 않는다. `.standalone-chrome`이 같은 위치를 덮는다.

## 탑앱바

- 높이: `52px`
- 제목: 중앙 고정, `17px / 700`, 한 줄
- 좌우 터치 영역: `40 × 40px`
- 좌우 여백: `10px`
- 아이콘 표시 크기: 뒤로가기 `21px`, 공유 `22px`, 홈 `24px`, 설정 `22px`
- 기본 배경은 흰색이며 하단에 `#eef0f4` 1px 구분선을 둔다.

## 동작 및 아이콘 배치

- 왼쪽에는 항상 뒤로가기를 둔다.
- 오른쪽에는 화면 고유 기능을 먼저 두고 홈을 가장 오른쪽에 둔다.
- 설정은 계정·보관함 관리 화면에만 사용한다.
- 공유는 결과 또는 외부 공유가 가능한 이벤트 화면에만 표시한다.
- 닫기(X)는 페이지 탑앱바가 아니라 모달·바텀시트 종료에만 사용한다.
- 피그마 이미지에 들어 있는 뒤로가기·홈·공유·설정 아이콘 위에 새 아이콘을 겹치지 않는다. 이미지 아이콘은 `.standalone-chrome`으로 가리고 공통 SVG만 표시한다.

## 구현 규칙

- 코드 네이티브 화면은 `.flow-subheader`, `.account-subheader`, `.drink-subheader`, `.shared-cart-subheader`의 공통 토큰을 따른다.
- 전체 화면 피그마 아트워크는 `createStandaloneChrome()`으로 동일한 스테이터스바와 `.app-topbar`를 주입한다.
- 새 페이지에서 임의의 `top`, `left`, 원형 흰 배경, 피그마에서 잘라낸 아이콘을 추가하지 않는다.
- 클릭 목표는 최소 `40 × 40px`, 키보드 포커스 링은 파란색 2px로 유지한다.
